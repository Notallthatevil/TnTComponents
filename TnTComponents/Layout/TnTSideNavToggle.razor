@namespace TnTComponents
@using TnTComponents.Core
@using TnTComponents.Interfaces
@inherits TnTComponentBase

<button @attributes="AdditionalAttributes"
        class="@ElementClass"
        style="@ElementStyle"
        lang="@ElementLang"
        id="@ElementId"
        title="@ElementTitle"
        onclick="TnTComponents.toggleSideNav(event)"
        @ref="Element" >

    @Icon.Render()
    @if (EnableRipple) {
        <TnTRippleEffect />
    }

</button> 
