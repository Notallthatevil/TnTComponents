@namespace TnTComponents

<style class="tnt-measurements">
    :root {
        --tnt-header-height: @($"{HeaderHeight}px;");
        --tnt-footer-height: @($"{FooterHeight}px;");
        --tnt-side-nav-width: @($"{SideNavWidth}px;");
    }
</style>