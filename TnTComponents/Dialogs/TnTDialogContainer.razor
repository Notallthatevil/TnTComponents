@*@inherits TnTBaseComponent
@inject TnTDialogService DialogService;

<div class="@GetCssClass()" theme="@Theme">
    @if(Dialog.Options.CloseOnExternalClick) {
        <TnTExternalClickHandler Callback="Close">
            @_content
        </TnTExternalClickHandler>
    }
    else {
        @_content
    }

</div>

*@