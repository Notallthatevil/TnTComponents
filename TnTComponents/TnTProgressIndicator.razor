@inherits TnTComponentBase
<progress @attributes="AdditionalAttributes"
          max="@Max"
          value="@Value"
          class="@GetClass()"
          style="@GetStyle()"
          theme="@Theme">
    @ChildContent
</progress>

 