@using System.ComponentModel.DataAnnotations
@using TnTComponents
@rendermode InteractiveAuto
<TnTForm Model="@_model" Appearance="FormAppearance.Outlined">
    <DataAnnotationsValidator />

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea @bind-Value="@_model.S" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea StartIcon="MaterialIcon.Gif" @bind-Value="@_model.S" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea @bind-Value="@_model.S" EndIcon="MaterialIcon.Group" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea StartIcon="MaterialIcon.Gif" @bind-Value="@_model.S" EndIcon="MaterialIcon.Group" />
    </TnTLabel>

</TnTForm>

<TnTForm Model="@_model" Appearance="FormAppearance.Filled">
    <DataAnnotationsValidator />

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea @bind-Value="@_model.S" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea StartIcon="MaterialIcon.Gif" @bind-Value="@_model.S" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea @bind-Value="@_model.S" EndIcon="MaterialIcon.Group" />
    </TnTLabel>

    <TnTLabel Label="My Label" BackgroundColor="TnTColor.Surface" Appearance="FormAppearance.Filled">
        <TnTInputTextArea StartIcon="MaterialIcon.Gif" @bind-Value="@_model.S" EndIcon="MaterialIcon.Group" />
    </TnTLabel>

</TnTForm>

<TnTInputCurrency @bind-Value="_currencyValue"/>

Currency
@(_currencyValue)
Value

@code {

    private Model _model = new();

    private decimal? _currencyValue;

    class Model {
        public Test? A { get; set; }
        public string S{ get; set; }
        public bool C{ get; set; }
        public Test? D{ get; set; }
    }

    enum Test {
        One,
        Two,
        Three,
        Four
    }

}